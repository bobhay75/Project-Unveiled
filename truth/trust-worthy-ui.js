(()=>{'use strict';const reduce=matchMedia('(prefers-reduced-motion: reduce)').matches;const opened=document.getElementById('questionOpenedAt');if(opened)opened.value=Math.floor(Date.now()/1000);document.querySelectorAll('.mode').forEach(btn=>btn.addEventListener('click',()=>{document.querySelectorAll('.mode').forEach(x=>x.classList.remove('active'));btn.classList.add('active');const mode=document.getElementById('investigationMode');if(mode)mode.value=btn.dataset.mode||'quick';const go=document.querySelector('.command-go');if(go)go.firstChild.textContent=btn.dataset.mode==='deep'?'START DEEP INVESTIGATION ':'INITIATE INVESTIGATION '}));document.querySelectorAll('#network button').forEach(btn=>btn.addEventListener('click',()=>{document.querySelectorAll('#network button').forEach(x=>x.classList.remove('active'));btn.classList.add('active');const out=document.getElementById('nodeTip');if(out)out.textContent=btn.dataset.tip||'Evidence node';}));if(!reduce&&matchMedia('(pointer:fine)').matches){document.querySelectorAll('.tilt').forEach(el=>{el.addEventListener('pointermove',e=>{const r=el.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;el.style.transform=`perspective(1000px) rotateX(${-y*1.8}deg) rotateY(${x*2.2}deg) translateY(-1px)`});el.addEventListener('pointerleave',()=>el.style.transform='')})}const canvas=document.getElementById('evidenceField');if(!canvas||reduce)return;const ctx=canvas.getContext('2d',{alpha:true});if(!ctx)return;let w=0,h=0,dpr=1,nodes=[];function resize(){dpr=Math.min(devicePixelRatio||1,1.5);w=innerWidth;h=innerHeight;canvas.width=Math.floor(w*dpr);canvas.height=Math.floor(h*dpr);canvas.style.width=w+'px';canvas.style.height=h+'px';ctx.setTransform(dpr,0,0,dpr,0,0);const count=Math.min(48,Math.max(20,Math.floor(w/32)));nodes=Array.from({length:count},()=>({x:Math.random()*w,y:Math.random()*h,vx:(Math.random()-.5)*.13,vy:(Math.random()-.5)*.13,r:Math.random()*1.4+.5}))}function frame(){ctx.clearRect(0,0,w,h);for(const p of nodes){p.x+=p.vx;p.y+=p.vy;if(p.x<0||p.x>w)p.vx*=-1;if(p.y<0||p.y>h)p.vy*=-1;ctx.beginPath();ctx.arc(p.x,p.y,p.r,0,Math.PI*2);ctx.fillStyle='rgba(76,190,255,.42)';ctx.fill()}for(let i=0;i<nodes.length;i++)for(let j=i+1;j<nodes.length;j++){const a=nodes[i],b=nodes[j],dx=a.x-b.x,dy=a.y-b.y,d=Math.hypot(dx,dy);if(d<145){ctx.beginPath();ctx.moveTo(a.x,a.y);ctx.lineTo(b.x,b.y);ctx.strokeStyle=`rgba(56,164,224,${(1-d/145)*.13})`;ctx.lineWidth=.7;ctx.stroke()}}requestAnimationFrame(frame)}addEventListener('resize',resize,{passive:true});resize();requestAnimationFrame(frame)})();
