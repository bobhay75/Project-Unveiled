(()=>{'use strict';const form=document.getElementById('investigationForm');if(!form)return;const workspace=document.getElementById('workspace'),resultPanel=document.getElementById('resultPanel'),status=document.getElementById('systemStatus'),begin=document.getElementById('beginButton'),activity=document.getElementById('activity'),sourceCount=document.getElementById('sourceCount'),counterCount=document.getElementById('counterCount'),receiptCount=document.getElementById('receiptCount'),verdict=document.getElementById('verdict'),probability=document.getElementById('probability'),probabilityNote=document.getElementById('probabilityNote'),analysis=document.getElementById('analysis'),sourceList=document.getElementById('sourceList');let receipts=0,uniqueSources=new Map(),counterSources=0;const log=(title,detail='')=>{const row=document.createElement('div');row.className='event';const b=document.createElement('b');b.textContent=title;row.appendChild(b);if(detail){const s=document.createElement('span');s.textContent=detail;row.appendChild(s)}activity.prepend(row)};const stageState=(name,state)=>{const item=document.querySelector(`[data-stage="${name}"]`);if(!item)return;item.classList.remove('active','done');if(state)item.classList.add(state)};const markActive=name=>{document.querySelectorAll('#stageList li.active').forEach(el=>el.classList.remove('active'));stageState(name,'active')};const safeUrl=value=>{try{const u=new URL(value,location.origin);return /^https?:$/.test(u.protocol)?u.href:''}catch{return''}};const consumeReceipt=r=>{if(!r||typeof r!=='object')return;receipts++;receiptCount.textContent=String(receipts);stageState(r.stage,'done');const order=['decompose','origin','primary','corroboration','counter','context','synthesis'];const idx=order.indexOf(r.stage);if(idx>=0&&idx<order.length-1)markActive(order[idx+1]);if(Array.isArray(r.sources)){r.sources.forEach(src=>{if(src&&src.url)uniqueSources.set(src.url,src)});sourceCount.textContent=String(uniqueSources.size);if(r.stage==='counter'){counterSources=r.sources.length;counterCount.textContent=String(counterSources)}}let detail='';if(Number.isFinite(r.source_count))detail=`${r.source_count} source${r.source_count===1?'':'s'} attached to this receipt`;if(r.evidence_floor_met===false)detail='Evidence floor not met — probability blocked';log(r.label||r.stage||'Stage completed',detail)};const renderComplete=result=>{resultPanel.hidden=false;status.textContent='COMPLETE';verdict.textContent=result.verdict||'UNRESOLVED';probability.textContent=Number.isFinite(result.probability)?`${result.probability}%`:'NO SCORE';probabilityNote.textContent=Number.isFinite(result.probability)?'Evidence-conditioned experimental probability. Open the evidence trail before trusting the number.':'Trust-Worthy refused to issue a probability because the evidence floor was not met.';analysis.textContent=result.analysis||'';sourceList.textContent='';(Array.isArray(result.sources)?result.sources:[]).forEach(src=>{const href=safeUrl(src.url||'');if(!href)return;const a=document.createElement('a');a.href=href;a.target='_blank';a.rel='noopener noreferrer';a.textContent=src.title||href;sourceList.appendChild(a)});resultPanel.scrollIntoView({behavior:'smooth',block:'start'})};const parseStream=async response=>{if(!response.ok){let msg=`Investigation request failed (${response.status})`;try{const body=await response.json();if(body&&body.message)msg=body.message}catch{}throw new Error(msg)}if(!response.body)throw new Error('Streaming response unavailable.');const reader=response.body.getReader(),decoder=new TextDecoder();let buffer='';for(;;){const {value,done}=await reader.read();buffer+=decoder.decode(value||new Uint8Array(),{stream:!done});let lines=buffer.split('\n');buffer=lines.pop()||'';for(const line of lines){if(!line.trim())continue;let event;try{event=JSON.parse(line)}catch{continue}if(event.type==='start'){status.textContent='INVESTIGATING';markActive('decompose');log(event.label||'Investigation started')}else if(event.type==='receipt')consumeReceipt(event.receipt);else if(event.type==='complete')renderComplete(event.result||{});else if(event.type==='error')throw new Error(event.message||'Investigation failed.')}if(done)break}};form.addEventListener('submit',async e=>{e.preventDefault();begin.disabled=true;workspace.hidden=false;resultPanel.hidden=true;activity.textContent='';receipts=0;uniqueSources=new Map();counterSources=0;sourceCount.textContent='0';counterCount.textContent='0';receiptCount.textContent='0';document.querySelectorAll('#stageList li').forEach(el=>el.classList.remove('active','done'));markActive('decompose');status.textContent='CONNECTING';const data=new URLSearchParams(new FormData(form));const goals=[...form.querySelectorAll('input[name="goal"]:checked')].map(x=>x.value);data.delete('goal');data.set('goals',goals.join('; '));try{const response=await fetch('/truth/deep-stream.php',{method:'POST',headers:{'Content-Type':'application/x-www-form-urlencoded;charset=UTF-8'},body:data.toString(),credentials:'same-origin'});await parseStream(response)}catch(err){status.textContent='STOPPED';log('Investigation stopped',err instanceof Error?err.message:'Unknown error')}finally{begin.disabled=false}});document.querySelectorAll('[data-branch]').forEach(btn=>btn.addEventListener('click',()=>{log('Branch requested',btn.dataset.branch||'Further investigation');btn.disabled=true;btn.textContent='Queued for V2 branch engine'}));})();