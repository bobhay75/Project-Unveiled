{
  "name": "oois-cloud-rule-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "firebase emulators:exec --config ../../firebase.emulator.json --project demo-oois-tests --only firestore,storage 'node --test rules.test.js'"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "4.0.1",
    "firebase": "11.10.0",
    "firebase-tools": "15.30.1"
  }
}
