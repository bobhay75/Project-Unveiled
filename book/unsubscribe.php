<?php
declare(strict_types=1);header('Content-Type:text/html; charset=UTF-8');header('Cache-Control:no-store,max-age=0');header('X-Robots-Tag:noindex,nofollow');
$token=trim((string)($_GET['token']??''));$file=dirname(__DIR__,2).'/site-private/project-unveiled/subscribers.json';$done=false;
if(strlen($token)>=32&&is_file($file)){$items=json_decode((string)file_get_contents($file),true);if(is_array($items)){foreach($items as &$r){if(is_array($r)&&hash_equals((string)($r['unsubscribe_token']??''),$token)){$r['status']='unsubscribed';$r['unsubscribed_at']=gmdate('c');$r['updated_at']=gmdate('c');$done=true;break;}}unset($r);if($done){$tmp=$file.'.tmp-'.bin2hex(random_bytes(4));$json=json_encode($items,JSON_PRETTY_PRINT|JSON_UNESCAPED_SLASHES);if($json===false||file_put_contents($tmp,$json,LOCK_EX)===false){$done=false;}else{@chmod($tmp,0640);if(!@rename($tmp,$file)){@unlink($tmp);$done=false;}}}}}
?><!doctype html><html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Project Unveiled Email Preferences</title><style>body{background:#080705;color:#f4ead2;font:18px/1.6 Arial;margin:0}main{max-width:760px;margin:12vh auto;padding:34px;border:1px solid #8a6a2c;background:#11100c}h1{font-family:Georgia;color:#e4b951}a{color:#e4b951}</style></head><body><main><h1><?= $done?'You are unsubscribed':'Link not found' ?></h1><p><?= $done?'You will not receive further Project Unveiled marketing emails. Your record is retained only as needed to honor this request.':'This unsubscribe link is invalid or expired.' ?></p><p><strong>We do not sell, rent, or trade your personal information. Ever.</strong></p><p><a href="/">Return to Project Unveiled</a></p></main></body></html>
